# dockerize-phalcon3.4

Cara menggunakan docker phalcon
- install docker
- install docker-compose
- jalankan : docker-compose -f phalcon.yaml up -d --build
- akses url di browser: http://localhost:8083/users
